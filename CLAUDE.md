# CLAUDE.md

This project's agent instructions live in [`AGENTS.md`](./AGENTS.md), not
here — Claude Code reads `AGENTS.md` natively. This file exists only so
tools that look specifically for `CLAUDE.md` still find their way there
immediately, without a second copy of the same content slowly drifting out
of sync with it.

Read `AGENTS.md` now. Everything else you need — architecture, security
posture, design direction, SEO standards, contribution conventions — is
linked from it.
