# جلوگیری کامل از دسترسی مستقیم مرورگر به این پوشه (فایل‌ها فقط از طریق require در PHP قابل استفاده‌اند)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
